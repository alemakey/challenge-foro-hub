# Puerto de la API
server.port=8080

# =====================
# Base de datos H2 (desarrollo en memoria)
# =====================
spring.datasource.url=jdbc:h2:mem:foro_hub;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway gestiona el esquema
spring.flyway.enabled=true

# Consola H2 para visualización (solo desarrollo)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# =====================
# Configuración MySQL (producción)
# Descomentar y configurar variables de entorno para usar MySQL
# =====================
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
#spring.jpa.hibernate.ddl-auto=none
#spring.flyway.enabled=true

# =====================
# Seguridad JWT
# =====================
# IMPORTANTE: Usar variable de entorno en producción
api.security.secret=${JWT_SECRET:clave-secreta-muy-larga-y-segura-para-desarrollo}
